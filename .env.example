# Stripe API Configuration
VITE_STRIPE_PUBLIC_KEY=your_publishable_key_here  # pk_live_* or pk_test_*

# IMPORTANT SECURITY NOTES:
# 1. Only publishable keys (pk_*) should be used in frontend code
# 2. Secret keys (sk_*) must ONLY be used on your backend server
# 3. Use test keys (pk_test_*) during development
# 4. Switch to live keys (pk_live_*) only in production
# 5. Never commit actual API keys to version control
